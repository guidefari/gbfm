pre-commit:
  parallel: false
  commands:
    format-and-lint:
      glob: '*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css}'
      run: bun oxfmt --write --no-error-on-unmatched-pattern {staged_files} && bun oxlint --fix --no-error-on-unmatched-pattern {staged_files} && bun oxfmt --write --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true

    format-svelte:
      glob: '*.svelte'
      run: bun prettier --write {staged_files}
      stage_fixed: true

    theme-css-build:
      glob: 'packages/theme/src/**/*.ts'
      run: cd packages/theme && bun run build:css && git add styles/theme.css
      exclude: 'packages/theme/src/build-css.ts'
# pre-push:
#   parallel: false
#   commands:
#     oxc-ci:
#       run: bun run oxc:ci

#     typecheck:
#       run: bun typecheck

# Optional: Uncomment to build before pushing (adds time but catches build errors early)
# build-www:
#   run: cd apps/www && bun run build

# commit-msg:
#   commands:
#     "commitlint":
#       run: echo "Commit message validated"
