name: Release

on:
  push:
    branches:
      - prod

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Automated Versioning
        uses: jveldboom/action-conventional-versioning@v1
        id: versioning

      - name: Update web package.json version
        run: |
          bun pm version ${{ steps.versioning.outputs.version }} --no-git-tag-version
          cd web && bun pm version ${{ steps.versioning.outputs.version }} --no-git-tag-version

      - name: Create Tag
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add package.json
          git commit -m "chore: bump package version to ${{ steps.versioning.outputs.version }}"
          git tag -a "v${{ steps.versioning.outputs.version }}" -m "Release v${{ steps.versioning.outputs.version }}"
          git push origin prod
          git push origin "v${{ steps.versioning.outputs.version }}"

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: "v${{ steps.versioning.outputs.version }}"
          name: "Release v${{ steps.versioning.outputs.version }}"
          body: "Automated release notes"
