name: Prod Deployment

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref to deploy (leave blank to use latest tag)'
        required: false
        default: ''
        type: string
      refresh:
        description: 'Run sst refresh before deploying (fixes state drift, e.g. Cloudflare ruleset error 20217)'
        required: false
        default: false
        type: boolean
      cf_ruleset_import:
        description: 'Cloudflare ruleset import ID, e.g. zones/{zone_id}/{ruleset_id}'
        required: false
        default: ''
        type: string

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-24.04
    env:
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Initial checkout to determine latest tag
        if: inputs.ref == ''
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get latest tag
        id: get-latest-tag
        if: inputs.ref == ''
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "prod")
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "🏷️Latest tag: $LATEST_TAG"

      - name: Determine deployment ref
        id: deployment-ref
        run: |
          REF="${{ inputs.ref || steps.get-latest-tag.outputs.latest_tag || github.ref }}"
          echo "ref=$REF" >> $GITHUB_OUTPUT
          echo "Deploying ref: $REF"

      - name: Checkout deployment ref
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.deployment-ref.outputs.ref }}
          fetch-depth: 0

      - uses: ./.github/actions/setup-bun-workspace

      - name: Configure Production AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{secrets.AWS_ROLE_ARN}}
          role-duration-seconds: 3600 #adjust as needed for your build time
          aws-region: us-east-1

      # SST Ion manages its own Pulumi binaries. The system-wide ones pre-installed in
      # the GH Actions runner can be outdated and cause "flag provided but not defined: -root" errors.
      - name: Remove system-wide Pulumi
        run: sudo rm -f /usr/local/bin/pulumi*

      - name: Deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_DEFAULT_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_DEFAULT_ACCOUNT_ID }}
          CF_RULESET_IMPORT: ${{ inputs.cf_ruleset_import }}
          SENTRY_RELEASE: ${{ steps.deployment-ref.outputs.ref }}

        run: |
          if [ "${{ inputs.refresh }}" = "true" ]; then
            bun run sst refresh --stage=prod
          fi
          bun run sst deploy --stage=prod

      - name: Publish Sentry release metadata
        if: ${{ env.SENTRY_AUTH_TOKEN != '' }}
        # getsentry/action-release v3.7.0
        uses: getsentry/action-release@ff07929a6537bac57790c3451cf4d364aca38528
        env:
          SENTRY_RELEASE: ${{ steps.deployment-ref.outputs.ref }}
          SENTRY_ORG: goosebumps-collective
          SENTRY_PROJECT: gbfm-frontend
        with:
          environment: production
          release: ${{ steps.deployment-ref.outputs.ref }}
          set_commits: auto
          finalize: true

      - name: Verify production deployment and telemetry
        timeout-minutes: 20
        env:
          AWS_REGION: us-east-1
          PRODUCTION_BASE_URL: https://vps.goosebumps.fm
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_OBSERVABILITY_TOKEN }}
          SENTRY_ENVIRONMENT: production
          SENTRY_ORG: goosebumps-collective
          SENTRY_PROJECT_ID: '4511355794227200'
          SENTRY_RELEASE: ${{ steps.deployment-ref.outputs.ref }}
          SST_APP: gbfm
          SST_STAGE: prod
        run: bun run apps/vps/scripts/verify-production-deployment.ts
